---
title: "Final Project_Predictive Analytics"
author: "Juhyun"
date: "`r Sys.Date()`"
output: pdf_document
---

### Libraries
```{r}
library(dplyr)
library(ggplot2)
library(tidyverse)
library(modelr)
```

### Import Dataset
```{r}
data <- read.csv("climate_change_impact_on_agriculture_2024.csv")
```

### Preparation and cleaning the data

```{r}
# mutate continent columns

country_to_continent <- data.frame(
  Country = c("Argentina", "Australia", "Brazil","Canada","China","France",
              "India","Nigeria", "Russia","USA"),
  Continent = c("South America", "Oceania", "South America", 
                "North America", "East Asia", "Europe", 
                "South Asia", "Africa","Eurasia","North America")
)

data_with_continent <- data %>%
  left_join(country_to_continent, by = "Country") 

data <- data_with_continent %>%
  select(Year, Country, Continent, Region, everything())

```

```{r}
set.seed(10000)

train_df <- data %>% sample_frac(0.7)

test_df <- anti_join(data, train_df)

```
```{r}
train_df<-train_df%>%
 mutate(
 continent = as.factor(Continent),
 crop_type = as.factor(Crop_Type),
 adaptation_strategies = as.factor(Adaptation_Strategies),
 average_temperature_c = as.numeric(Average_Temperature_C),
 extreme_weather_events = as.logical(Extreme_Weather_Events),
 crop_yield_mt_per_ha = as.numeric(Crop_Yield_MT_per_HA)
 )
```
```{r}
 train_df%>%
 pivot_longer(cols= Year|Average_Temperature_C, names_to="variable",values_to="value")%>%
 ggplot()+
 geom_histogram(
 mapping=aes(x=value,fill=extreme_weather_events),
 position="identity",
 alpha=0.5)+
 facet_wrap(~ variable,scales = "free")+
 labs(title="HistogramofAgeandFareandwhethertheysurvived",
 x= "value",
 y= "count"
 )
```
```{r}
 train_df%>%
 pivot_longer(cols= Year|Crop_Yield_MT_per_HA, names_to="variable",values_to="value")%>%
 ggplot()+
 geom_histogram(
 mapping=aes(x=value,fill= extreme_weather_events),
 position="identity",
 alpha=0.5)+
 facet_wrap(~ variable,scales = "free")+
 labs(title="HistogramofAgeandFareandwhethertheysurvived",
 x= "value",
 y= "count"
 )
```
```{r, fig.size = 10}
 train_df %>%
 pivot_longer(cols = c(Year|Crop_Yield_MT_per_HA), names_to = "variable",
 values_to = "value")%>%
 ggplot()+
 geom_histogram(mapping=aes(x=value,fill=extreme_weather_events)) +
 facet_wrap(~ variable, scales= "free")+
 labs(title="Bar graph of survival by Parch, Pclass, Sex, SibSp",
 x = "value",
 y= "count")
```
```{r}
 train_df %>%
 summarize(
 total = n(),
 missing = sum(is.na(Crop_Yield_MT_per_HA)),
 fraction_missing = missing/total
 )
```
```{r}
model_1<-glm(extreme_weather_events~Crop_Yield_MT_per_HA ,data=train_df,family="binomial")
```

```{r}
 model_1_preds<-train_df %>%
 add_predictions(model_1,type="response")%>%
 mutate(
 outcome= if_else(condition=pred>0.5,
 true=1,
 false=0)
 )
```
```{r}
 model_1_preds %>%
 mutate(
 correct= if_else(
 condition=Extreme_Weather_Events== outcome,
 true =1,
 false=0
 )
 )%>%
 summarize(
 total_correct=sum(correct),
 accuracy=total_correct/n()
 )
```
```{r}
library(boot)
logistic_cv1 <- cv.glm(
  data = train_df,
  glmfit = model_1,
  K = 5
)
```


```{r}
logistic_cv1$delta
```


```{r}
model_2<-glm(extreme_weather_events~Crop_Yield_MT_per_HA * Average_Temperature_C * Total_Precipitation_mm * CO2_Emissions_MT ,data=train_df,family="binomial")
```

```{r}
 model_2_preds<-train_df %>%
 add_predictions(model_2,type="response")%>%
 mutate(
 outcome= if_else(condition=pred>0.5,
 true=1,
 false=0)
 )
```
```{r}
 model_2_preds %>%
 mutate(
 correct= if_else(
 condition=Extreme_Weather_Events== outcome,
 true =1,
 false=0
 )
 )%>%
 summarize(
 total_correct=sum(correct),
 accuracy=total_correct/n()
 )
```
```{r}
library(boot)
logistic_cv2 <- cv.glm(
  data = train_df,
  glmfit = model_2,
  K = 5
)
```


```{r}
logistic_cv2$delta
```

```{r}
model_3<-glm(extreme_weather_events ~ (Average_Temperature_C + Total_Precipitation_mm + CO2_Emissions_MT) * Crop_Yield_MT_per_HA ,data=train_df,family="binomial")
```

```{r}
 model_3_preds<-train_df %>%
 add_predictions(model_3,type="response")%>%
 mutate(
 outcome= if_else(condition=pred>0.5,
 true=1,
 false=0)
 )
```
```{r}
 model_3_preds %>%
 mutate(
 correct= if_else(
 condition=Extreme_Weather_Events== outcome,
 true =1,
 false=0
 )
 )%>%
 summarize(
 total_correct=sum(correct),
 accuracy=total_correct/n()
 )
```
```{r}
library(boot)
logistic_cv3 <- cv.glm(
  data = train_df,
  glmfit = model_3,
  K = 5
)
```


```{r}
logistic_cv3$delta
```

```{r}
test_df<-test_df%>%
 mutate(
 continent = as.factor(Continent),
 crop_type = as.factor(Crop_Type),
 adaptation_strategies = as.factor(Adaptation_Strategies),
 average_temperature_c = as.numeric(Average_Temperature_C),
 extreme_weather_events = as.logical(Extreme_Weather_Events),
 crop_yield_mt_per_ha = as.numeric(Crop_Yield_MT_per_HA)
 )
```
```{r}
model_best <-glm(extreme_weather_events ~ Crop_Yield_MT_per_HA,data= test_df,
                  family="binomial")
```
```{r}
 model_best_preds<-test_df%>%
 add_predictions(model_best,type="response")%>%
 mutate(
 outcome= if_else(condition=pred>0.5,
 true=1,
 false=0)
 )
```
```{r}
logistic_cv_best <- cv.glm(
  data = test_df,
  glmfit = model_best,
  K = 5
)
```


```{r}
logistic_cv3$delta
```